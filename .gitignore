# Secrets — ne jamais committer
.env

# Système / éditeurs
node_modules/
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store

# Auth OTP — secrets & état serveur
api/lib/config.php
api/lib/config.prod.php
data.json
api/state/

# Base de données — artefacts de seed générés (régénérables via node db/seed.mjs)
db/seed.sql
souvenirs/*/seed/

# Scratch subagent-driven-dev (briefs, rapports, ledger — contient des secrets)
.superpowers/

# Manifeste de déploiement incrémental (état local, régénérable)
.deploy-manifest.json
